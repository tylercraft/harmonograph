// Copy this file to config.js and fill in your values.
// config.js is gitignored — never commit it.
//
// User preferences (Linear API key, theme, clocks) are stored
// per-device in chrome.storage.local via the settings panel (⚙).

const CONFIG = {
  // Set to true to use stub data — no APIs, no auth required
  stub: false,

  // Google OAuth client ID from Google Cloud Console.
  // Application type: Chrome Extension. Enter the extension ID from chrome://extensions.
  googleOAuthClientId: '',
};
